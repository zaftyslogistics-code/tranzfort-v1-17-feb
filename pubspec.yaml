name: tranzfort
description: "TranZfort — India's trucking network. Connect suppliers with truckers."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # Core
  supabase_flutter: ^2.5.6
  flutter_riverpod: ^2.5.1
  go_router: ^14.2.0
  google_fonts: ^6.2.1
  riverpod_annotation: ^2.3.5
  intl: ^0.20.2
  uuid: ^4.4.0

  # UI/UX
  shimmer: ^3.0.0
  cached_network_image: ^3.3.1
  flutter_animate: ^4.5.0
  smooth_page_indicator: ^1.1.0
  flutter_svg: ^2.0.10
  connectivity_plus: ^6.0.3
  cupertino_icons: ^1.0.8

  # Voice & TTS
  flutter_tts: ^4.0.2
  record: ^6.0.0
  just_audio: ^0.9.39

  # Permissions
  permission_handler: ^11.3.1

  # Utilities
  share_plus: ^9.0.0
  url_launcher: ^6.3.0
  image_picker: ^1.1.2
  shared_preferences: ^2.2.3
  path_provider: ^2.1.3
  collection: ^1.18.0
  geolocator: ^10.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.4.8
  riverpod_generator: ^2.4.0
  riverpod_lint: ^2.3.10
  custom_lint: ^0.6.4
  flutter_launcher_icons: ^0.14.3

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/icon-d.png"
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/images/icon-d.png"

flutter:
  generate: true
  uses-material-design: true

  assets:
    - assets/images/
    - assets/svg/
    - assets/data/
